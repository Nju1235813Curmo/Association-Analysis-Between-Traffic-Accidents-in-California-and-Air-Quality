加州空气质量与交通事故关联分析项目 - 环境搭建说明文档

文档概述
   本文档用于指导完成 “加州空气质量与交通事故关联分析” 项目的环境搭建，包含依赖安装、数据准备、配置验证等全流程步骤，适配 Windows/macOS/Linux 系统，确保代码可正常运行。

一、环境搭建前置要求
1. 基础硬件要求
内存：≥8GB（处理大数据集需≥16GB）
硬盘：预留≥10GB 空间（存储数据集与输出文件）
网络：需联网下载依赖库与数据集
2. 基础软件要求
Python 版本：3.8~3.11（推荐 3.9 版本，兼容性最佳）
下载地址：Python 官网
安装注意：
Windows：勾选 “Add Python to PATH”，避免手动配置环境变量
macOS/Linux：系统自带 Python 可能版本过低，建议通过 Miniconda 安装指定版本


二、核心文件准备
在开始搭建前，需先准备以下 3 个核心文件，并存放在同一项目文件夹（如命名为 CA_AirAccident_Analysis）中：
文件名	作用	获取方式
requirements.txt：依赖库清单，指定需安装的库及版本
environment_config.py：项目配置文件，管理路径、参数等
数据集文件：交通事故数据与空气质量数据



三、依赖库安装
1. （推荐）创建虚拟环境
虚拟环境可避免依赖冲突，步骤如下：
bash
# 1. 安装虚拟环境管理工具（若未安装）
pip install virtualenv

# 2. 进入项目文件夹（示例路径，需替换为你的实际路径）
# Windows
cd C:\Users\YourName\CA_AirAccident_Analysis
# macOS/Linux
cd /Users/YourName/CA_AirAccident_Analysis

# 3. 创建虚拟环境（命名为 venv）
virtualenv venv

# 4. 激活虚拟环境
# Windows（命令提示符）
venv\Scripts\activate.bat
# Windows（PowerShell）
.\venv\Scripts\Activate.ps1
# macOS/Linux
source venv/bin/activate
激活成功标志：命令行前缀出现 (venv)。
2. 安装依赖库
在项目文件夹中创建 requirements.txt 文件，内容如下：
txt
# 核心数据处理库
pandas>=1.5.0,<2.1.0
numpy>=1.21.0,<1.26.0

# 可视化库
matplotlib>=3.5.0,<3.8.0
seaborn>=0.12.0,<0.13.0
plotly>=5.10.0,<5.18.0
folium>=0.14.0,<0.15.0

# 机器学习库
scikit-learn>=1.0.0,<1.3.0
scipy>=1.7.0,<1.11.0

# Dash 交互仪表盘库
dash>=2.7.0,<2.14.0
dash-bootstrap-components>=1.2.0,<1.5.0

# 辅助工具库
pickle-mixin==1.0.0
psutil>=5.9.0
执行安装命令（虚拟环境已激活状态）：
bash
# 国内建议加清华源，加速下载
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 验证安装（可选）
pip list | findstr "pandas numpy dash scikit-learn"  # Windows
pip list | grep "pandas\|numpy\|dash\|scikit-learn"  # macOS/Linux



四、项目配置（environment_config.py）
在项目文件夹中创建 environment_config.py 文件，用于统一管理路径、可视化参数等，代码如下（需根据你的实际情况修改路径）：
python
运行
#!/usr/bin/env python3
# 加州空气质量与交通事故分析 - 项目配置文件
import os

# ==================== 1. 数据路径配置（核心！需手动调整） ====================
class DataPath:
    # 项目根目录（自动获取，无需修改）
    ROOT = os.path.dirname(os.path.abspath(__file__))
    
    # 交通事故数据集路径（多个路径备选，确保至少一个存在）
    ACCIDENT_FILES = [
        os.path.join(ROOT, "US_Accidents_March23.csv"),  # 优先推荐的数据集文件
        os.path.join(ROOT, "US_Accidents.csv"),          # 备选文件1
        r"D:/Data/US_Accidents_March23.csv"              # 备选文件2（示例：Windows D盘路径）
    ]
    
    # 空气质量数据集路径（同上）
    AQI_FILES = [
        os.path.join(ROOT, "US_AQI.csv"),                # 优先推荐的数据集文件
        r"D:/Data/US_AQI.csv"                            # 备选文件（示例：Windows D盘路径）
    ]
    
    # 输出文件路径（图表、中间数据等，无需修改）
    CHART_SAVE_DIR = ROOT  # 图表保存到项目根目录
    MIDDLE_DATA_PATH = os.path.join(ROOT, "ca_data_full.pkl")  # 中间数据文件
    DASH_APP_PORT = 8050  # Dash 仪表盘启动端口（访问地址：http://127.0.0.1:8050）

# ==================== 2. 可视化参数配置（解决中文乱码） ====================
class VisualConfig:
    # 中文字体路径（根据系统选择，注释掉不适用的）
    # Windows 系统（默认，无需修改）
    SIMHEI_FONT_PATH = "C:\\Windows\\Fonts\\simhei.ttf"
    # macOS 系统（需取消注释，并确保字体已安装）
    # SIMHEI_FONT_PATH = "/Library/Fonts/SimHei.ttf"
    # Linux 系统（需取消注释，执行 sudo apt-get install fonts-wqy-zenhei 安装）
    # SIMHEI_FONT_PATH = "/usr/share/fonts/truetype/wqy/wqy-zenhei.ttc"
    
    # 图表基础样式（无需修改）
    PLOT_STYLE = "seaborn-v0_8-darkgrid"  # 绘图风格
    CHART_DPI = 300  # 静态图表分辨率（高清）
    COLOR_MAP = {
        "light_blue": "#90CAF9",
        "mid_blue": "#42A5F5",
        "severe_red": "#E74C3C",  # 严重事故标记色
        "normal_blue": "#42A5F5"  # 普通事故标记色
    }

# ==================== 3. 模型训练参数配置（确保结果可复现） ====================
class ModelConfig:
    RANDOM_SEED = 42  # 随机种子（固定后每次训练结果一致）
    TEST_SIZE = 0.2   # 测试集占比（20% 数据用于模型测试）
    SEVERE_THRESHOLD = 3  # 严重事故判定标准（Severity ≥ 3 为严重事故）
    CROSS_VAL_FOLDS = 5  # 交叉验证折数

# ==================== 4. 配置验证工具函数（无需修改） ====================
def check_data_exists():
    """检查数据集是否存在，输出结果提示"""
    # 检查交通事故数据
    acc_exists = any(os.path.exists(file) for file in DataPath.ACCIDENT_FILES)
    # 检查空气质量数据
    aqi_exists = any(os.path.exists(file) for file in DataPath.AQI_FILES)
    
    print("=" * 50)
    print("数据集存在性检查结果：")
    print(f"交通事故数据：{'已找到' if acc_exists else '未找到（请检查 DataPath.ACCIDENT_FILES 路径）'}")
    print(f"空气质量数据：{'已找到' if aqi_exists else '未找到（请检查 DataPath.AQI_FILES 路径）'}")
    print("=" * 50)

def get_chinese_font():
    """获取可用的中文字体路径，避免中文乱码"""
    if os.path.exists(VisualConfig.SIMHEI_FONT_PATH):
        return VisualConfig.SIMHEI_FONT_PATH
    else:
        raise FileNotFoundError(f"中文字体未找到！请检查 VisualConfig.SIMHEI_FONT_PATH 配置")
        

五、数据准备
1. 下载数据集
从 Kaggle 下载以下两个核心数据集，保存到项目根目录（或修改 DataPath 中的路径指向数据集位置）：
交通事故数据：US Accidents
推荐下载 US_Accidents_March23.csv（最新且数据量适中）
空气质量数据：US Air Quality 1980-Present
推荐下载 US_AQI.csv（包含完整的加州空气质量数据）
2. 验证数据路径
在项目文件夹中创建 check_config.py 文件，执行以下代码验证数据是否找到：
python
运行
# 验证数据路径与中文字体配置
from environment_config import check_data_exists, get_chinese_font

# 检查数据集是否存在
check_data_exists()

# 检查中文字体是否可用
try:
    font_path = get_chinese_font()
    print(f"中文字体配置：可用，路径：{font_path}")
except FileNotFoundError as e:
    print(f"中文字体配置：{e}")
运行命令：python check_config.py，若输出 “已找到” 和 “可用”，则配置正常；若提示 “未找到”，需重新检查数据集路径。



六、中文字体配置（解决图表中文乱码）
1. Windows 系统
无需额外操作：系统默认自带 “SimHei（黑体）” 字体，VisualConfig 中已配置路径。
2. macOS 系统
下载 SimHei 字体
双击字体文件，点击 “安装字体”
修改 environment_config.py 中的 VisualConfig，取消注释 macOS 字体路径：
python
运行
# SIMHEI_FONT_PATH = "C:\\Windows\\Fonts\\simhei.ttf"  # 注释 Windows 路径
SIMHEI_FONT_PATH = "/Library/Fonts/SimHei.ttf"         # 启用 macOS 路径
3. Linux 系统
安装中文字体：sudo apt-get install fonts-wqy-zenhei（Ubuntu/Debian 系统）
修改 environment_config.py 中的 VisualConfig，取消注释 Linux 字体路径：
python
运行
# SIMHEI_FONT_PATH = "C:\\Windows\\Fonts\\simhei.ttf"  # 注释 Windows 路径
SIMHEI_FONT_PATH = "/usr/share/fonts/truetype/wqy/wqy-zenhei.ttc"  # 启用 Linux 路径



七、环境验证
完成上述步骤后，运行以下代码验证全环境是否正常：
python
运行
# 环境验证脚本（save as test_environment.py）
from environment_config import DataPath, VisualConfig, ModelConfig
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns
from sklearn.linear_model import LogisticRegression
from dash import Dash

# 1. 验证依赖库导入
try:
    import pandas, numpy, matplotlib, seaborn, plotly, folium, sklearn, dash
    print("所有依赖库导入成功")
except ImportError as e:
    print(f"依赖库导入失败：{e}")

# 2. 验证数据读取
try:
    # 读取交通事故数据（取第一个存在的文件）
    acc_file = next(f for f in DataPath.ACCIDENT_FILES if os.path.exists(f))
    acc_df = pd.read_csv(acc_file, nrows=100)  # 读取前 100 行验证
    print(f"交通事故数据读取成功，文件：{acc_file}，数据量：{len(acc_df)} 行")
    
    # 读取空气质量数据
    aqi_file = next(f for f in DataPath.AQI_FILES if os.path.exists(f))
    aqi_df = pd.read_csv(aqi_file, nrows=100)
    print(f"空气质量数据读取成功，文件：{aqi_file}，数据量：{len(aqi_df)} 行")
except StopIteration:
    print("数据文件未找到，请检查 DataPath 配置")
except Exception as e:
    print(f"数据读取失败：{e}")

# 3. 验证可视化配置
try:
    plt.rcParams['font.sans-serif'] = [VisualConfig.SIMHEI_FONT_PATH]
    plt.rcParams['axes.unicode_minus'] = False
    plt.figure(figsize=(6, 4))
    plt.plot([1, 2, 3], [4, 5, 6], label="测试曲线")
    plt.title("中文标题测试")
    plt.legend()
    plt.close()
    print("可视化配置（中文字体）正常")
except Exception as e:
    print(f"可视化配置失败：{e}")

# 4. 验证模型初始化
try:
    model = LogisticRegression(random_state=ModelConfig.RANDOM_SEED)
    print("机器学习模型初始化正常")
except Exception as e:
    print(f"模型初始化失败：{e}")

# 5. 验证 Dash 初始化
try:
    app = Dash(__name__)
    print(f"Dash 仪表盘初始化正常，启动端口：{DataPath.DASH_APP_PORT}")
except Exception as e:
    print(f"Dash 初始化失败：{e}")
运行命令：python test_environment.py，若所有步骤均输出 “✅”，则环境搭建完成。



八、代码运行步骤
运行数据处理与基础分析代码(analysis_visualization.py)（生成 ca_data_full.pkl 中间数据）
运行模型对比分析代码（model.py）（生成模型评估图表）
运行 Dash 仪表盘代码（命令：python dashboard.py），访问 http://127.0.0.1:8050 查看交互界面 ctrl+c关闭页面。

