# 加州空气质量与交通事故关联分析 - 数据集描述文档

    本文档详细描述项目所用的两个核心数据集——US Accidents（美国交通事故数据集）与 US Air Quality 1980-Present（美国空气质量数据集）的来源、结构、字段含义及数据预处理逻辑，结合代码运行结果补充具体数据，为后续分析提供完整数据基础说明。

## 一、数据集基本信息总览
- **US Accidents**：数据来源于Kaggle，覆盖美国全境且包含多时间维度，核心用途是分析加州交通事故的时空分布与影响因素，项目优先使用文件为US_Accidents_March23.csv。
- **US Air Quality 1980-Present**：数据来源于Kaggle，覆盖美国全境且时间范围从1980年至今，核心用途是分析加州空气质量（AQI）变化趋势，项目优先使用文件为US_AQI.csv。



## 二、US Accidents 数据集详细描述
### 1. 数据集来源与背景
该数据集由Kaggle用户Sobhan Moosavi整理发布，包含美国全境从2016年至今的交通事故记录。数据来源于交通管理部门、天气服务及第三方交通监测平台，是目前公开的规模最大、维度最丰富的美国交通事故数据集之一。数据来源：https://www.kaggle.com/datasets/sobhanmoosavi/us-accidents

### 2. 数据文件与规模
- 优先使用文件：US_Accidents_March23.csv，该文件包含截至2023年3月的最新数据。
- 项目实际数据规模：加载后加州交通事故记录数为1,741,433条，经清洗后有效记录数仍为1,741,433条（无无效记录需剔除）。

### 3. 核心字段含义（项目中实际使用的列）
- **ID**：字符串类型，为交通事故唯一标识（如“A-1”），用于数据去重与唯一索引。
- **Severity**：整数类型（取值1-4），代表事故严重程度，其中1=轻微、2=较轻、3=较严重、4=严重，作为分析目标变量（如“严重事故”判定）。
- **Start_Time**：datetime类型，格式为ISO8601（如“2016-02-08 05:46:00”），用于提取年、月、时、星期等时间特征。
- **End_Time**：datetime类型，代表事故结束时间（交通恢复正常的时间），用于计算事故持续时长（衍生特征）。
- **Start_Lat / Start_Lng**：浮点数类型，分别代表事故发生地点的纬度与经度，用于地理分布分析（热力图、地图标记）。
- **City / County / State**：字符串类型，分别代表事故所在城市、县、州（如“Dayton”/“Montgomery”/“OH”），用于筛选加州数据（State=CA）。
- **Weather_Condition**：字符串类型，代表事故发生时的天气状况（如“Light Rain”“Overcast”），用于分析天气对事故的影响。
- **Visibility(mi)**：浮点数类型，代表能见度（单位：英里），是核心分析变量（与事故严重度关联）。
- **Temperature(F)**：浮点数类型，代表温度（单位：华氏度），用于环境因素对事故的影响分析。
- **Humidity(%)**：浮点数类型，代表湿度（百分比），作为辅助环境因素分析。
- **Pressure(in)**：浮点数类型，代表气压（单位：英寸汞柱），作为辅助环境因素分析。
- **Wind_Speed(mph)**：浮点数类型，代表风速（单位：英里/小时），作为辅助环境因素分析。
- **Precipitation(in)**：浮点数类型，代表降水量（单位：英寸），用于分析降雨/降雪对事故的影响。

### 4. 数据预处理逻辑（项目中执行的清洗步骤）
1. **数据筛选**：仅保留State字段为“CA”的加州数据，排除其他州记录。
2. **时间格式处理**：将Start_Time和End_Time转换为标准datetime格式（优先ISO8601，兼容混合格式），删除时间字段为空（NaN）的无效记录。
3. **时间特征衍生**：从Start_Time提取Year（年）、Month（月）、Hour（时）、Date（日期）、DayOfWeek（星期几）。
4. **能见度数据清洗**：将Visibility(mi)转换为数值类型，无法转换的值设为NaN；移除异常值（能见度超过20英里的记录设为NaN）。
5. **缺失值处理**：仅保留核心字段（Start_Time、End_Time、Start_Lat、Start_Lng等）无缺失的记录。

### 5. 补充数据统计（基于代码运行结果）
- **时间范围**：事故记录时间从2016-03-22 17:44:13至2023-03-31 23:25:30。
- **覆盖城市数**：共覆盖1268个城市。
- **事故严重程度分布**：严重程度1有10,284起（占比0.6%）；严重程度2有1,445,833起（占比83.0%）；严重程度3有271,814起（占比15.6%）；严重程度4有13,502起（占比0.8%）。
- **能见度统计**：平均能见度9.06英里，中位数能见度10.00英里，最低能见度0.00英里，最高能见度20.00英里；低能见度（<1英里）事故共17,796起，平均严重程度2.10。
- **月度事故分布**：一月168,346起、二月157,121起、三月139,283起、四月139,258起、五月125,822起、六月131,105起、七月108,278起、八月124,470起、九月147,711起、十月147,271起、十一月165,006起、十二月187,762起。
- **事故高峰时段**：17:00时段事故最多，共122,358起。
- **工作日与周末对比**：工作日事故1,383,952起，平均严重度2.16；周末事故357,481起，平均严重度2.18。
- **事故最多的前10个城市**：依次为Los Angeles（156,491起）、Sacramento（66,264起）、San Diego（55,504起）、San Jose（34,531起）、Riverside（26,317起）、Oakland（23,571起）、San Bernardino（21,990起）、Bakersfield（21,390起）、Anaheim（21,293起）、Long Beach（21,040起）。



## 三、US Air Quality 1980-Present 数据集详细描述
### 1. 数据集来源与背景
该数据集由Kaggle用户Caleb Reigada整理发布，整合了美国环境保护署（EPA）从1980年至今的空气质量监测数据。包含全美各地的每日空气质量指数（AQI）及污染物浓度信息，是分析美国空气质量长期变化的核心数据集。数据来源：https://www.kaggle.com/datasets/calebreigada/us-air-quality-1980present

### 2. 数据文件与规模
- 优先使用文件：US_AQI.csv，为结构化数据，包含城市级每日AQI记录。
- 项目实际数据规模：加载后加州空气质量记录数为477,820条；数据包含Category列和state_id列，可直接用于筛选与分组分析。

### 3. 核心字段含义（基于提供的前10行数据及项目需求）
- **CBSA Code**：整数类型，为核心统计区域代码（美国官方地理分区标识），作为地理分区辅助标识（暂未直接使用）。
- **Date**：日期类型（如“2022-01-01”），用于与交通事故数据按“日期-城市”关联。
- **AQI**：整数类型（0-500），代表空气质量指数，数值越高污染越严重，是核心分析变量（空气质量衡量标准）。
- **Category**：字符串类型（如“Good”“Moderate”，对应中文“良好”“中等”），用于AQI等级分组分析（如“有害”vs“良好”）。
- **Defining Parameter**：字符串类型，代表主导AQI的污染物（如“PM2.5”“Ozone”），辅助分析污染物类型对事故的影响（暂用PM2.5）。
- **Number of Sites Reporting**：整数类型，代表报告该AQI的监测站点数量，作为数据可信度参考（站点数≥2视为有效）。
- **city_ascii**：字符串类型，为城市英文名称（ASCII格式，如“Aberdeen”），用于与交通事故数据的“City”字段匹配关联。
- **state_id / state_name**：字符串类型，分别代表州缩写（如“WA”）、州全称（如“Washington”），用于筛选加州数据（state_id=CA）。
- **lat / lng**：浮点数类型，分别代表城市中心点纬度、经度，用于地理分布辅助分析。
- **population**：浮点数类型，代表城市人口数量，作为人口密度对事故/空气质量的影响分析（备选）。
- **额外字段**：数据还包含“Unnamed: 0”“density”“timezone”列，暂未用于核心分析。

### 4. 数据预处理逻辑（项目中执行的清洗步骤）
1. **数据筛选**：优先按state_id=CA筛选加州数据；若无该字段，按state_name匹配“California”“california”等变体。
2. **有效记录保留**：保留AQI字段有效（0-500）的记录，排除异常值（如负数、超过500的值）。
3. **日期格式处理**：将Date转换为标准日期格式，删除日期为空的记录。
4. **字段标准化**：将city_ascii重命名为City，确保与交通事故数据的“City”字段名一致，便于关联；若存在多个监测站点的同日数据，按“City-Date”维度聚合，取AQI的平均值作为当日城市AQI。
5. **AQI等级分组**：基于AQI数值衍生AQI_Category字段，分组标准为：0-50（良好，无污染）、51-100（中等，轻微污染）、101-150（对敏感人群不健康，轻度污染）、151-200（不健康，中度污染）、201-300（非常不健康，重度污染）、>300（有害，严重污染）。

### 5. 补充数据统计（基于代码运行结果）
- **AQI统计**：平均AQI为63.8，最高AQI为20646.0（存在异常高值，需后续单独处理），最低AQI为0.0。
- **AQI分类分布**：良好264,420天、中等139,753天、对敏感人群不健康45,444天、不健康19,710天、非常不健康7,861天、有害344天；衍生AQI_Category列后，可直接用于分组分析。



## 四、数据集关联逻辑
项目核心是分析“空气质量-交通事故”的关联关系，需将两个数据集按“日期（Date）-城市（City）”维度合并，具体逻辑如下：
1. **交通事故数据聚合**：按“Date-City”分组，计算每日每个城市的事故总数（Accident_Count）、平均事故严重度（Avg_Severity）。
2. **空气质量数据聚合**：按“Date-City”分组，计算每日每个城市的平均AQI（Avg_AQI）。
3. **数据合并**：使用pd.merge()按“Date-City”进行内连接，仅保留两个数据集都有记录的“日期-城市”组合，确保分析样本的完整性。
4. **关联后数据统计**：成功关联的记录数为467,303条；AQI与事故严重程度相关系数为0.033，AQI与事故数量相关系数为-0.019，整体相关性较弱。



## 五、数据质量说明
### 1. 数据可信度
- **US Accidents**：数据来源于官方交通监测平台，时间精度到分钟，地理精度到经纬度，严重度标注符合行业标准，可信度高。
- **US Air Quality**：AQI数据由EPA认证的监测站点采集，污染物浓度检测方法统一，历史数据连续性强，可信度高。

### 2. 潜在局限性
- **时间范围差异**：交通事故数据始于2016年，空气质量数据始于1980年，仅2016年后的重叠时间范围可用于关联分析。
- **地理匹配偏差**：部分小城市可能存在“城市名拼写不一致”（如“Los Angeles”vs“LA”），导致关联时丢失少量数据。
- **缺失值问题**：交通事故数据的“能见度”“降水量”等字段存在少量缺失（约5%-10%），需通过均值填充或删除处理。
- **AQI异常值**：空气质量数据中存在AQI=20646.0的异常高值，需在后续分析中单独识别并处理，避免影响结果准确性。

